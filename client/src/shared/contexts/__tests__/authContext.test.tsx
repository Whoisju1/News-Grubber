import { shallow } from 'enzyme';
import {
  AuthContext,
  AuthProvider,
  IAuthCtx,
  IUser,
  IUserCredentials,
} from '../authContext';

describe('authContext', () => {
  it('should return the correct initial values', () => {

  });
});
