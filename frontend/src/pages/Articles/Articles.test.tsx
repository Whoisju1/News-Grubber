import { shallow, mount } from 'enzyme';
import Articles from './Articles';

describe('Articles component', () => {
  it.skip('should have state that consists of an array of articles', () => {
    // test later
  })
});