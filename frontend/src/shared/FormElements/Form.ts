import styled from 'styled-components';

export default styled.form`
  display: grid;
  background-color: #FFF;
  padding: 2rem;
  grid-gap: 1rem;
  border-radius: 5px;
  box-shadow: 0 3px 4rem rgba(0, 0, 0, .3);
`;
