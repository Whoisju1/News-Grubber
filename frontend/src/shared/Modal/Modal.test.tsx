import React from 'react'
import { shallow } from 'enzyme';
import Modal from './Modal';

describe('Modal Component', () => {
  const modal = shallow(<Modal show={true}>
    <div />
  </Modal>)
  it.skip('should', () => {

  });
});

